parameters:
    level: max
    paths:
        - src
        - tests
    ignoreErrors:
        # Data-provider methods inside tests/ may not document iterable value types per testing rules.
        - identifier: missingType.iterableValue
          path: tests/CurrencyTest.php
    reportUnmatchedIgnoredErrors: true
